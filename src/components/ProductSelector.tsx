'use client';

import { Box, Heading, Text } from '@chakra-ui/react';

const ProductSelector = () => {
  return (
    <Box p={8}>
      <Heading mb={4}>Product Selector</Heading>
      <Text>This component will allow floor staff to select a product and enter a quantity. Functionality coming soon.</Text>
    </Box>
  );
};

export default ProductSelector;